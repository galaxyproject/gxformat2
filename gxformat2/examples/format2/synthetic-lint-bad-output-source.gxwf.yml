class: GalaxyWorkflow
label: Bad output source
inputs:
  the_input:
    type: File
outputs:
  the_output:
    outputSource: nonexistent_step/out_file1
steps:
  cat:
    tool_id: cat1
    in:
      input1: the_input
