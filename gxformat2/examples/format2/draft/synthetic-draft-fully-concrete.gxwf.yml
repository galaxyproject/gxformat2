class: GalaxyWorkflowDraft
label: fully concrete draft — no TODOs, no _plan_*
inputs:
  reads:
    type: data
outputs:
  out:
    outputSource: cat/out_file1
steps:
  cat:
    tool_id: cat1
    tool_version: "1.0.0"
    label: cat
    in:
      input1: reads
    out:
      - id: out_file1
