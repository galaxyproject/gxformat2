class: GalaxyWorkflow
label: Report with bad markdown type
inputs:
  the_input:
    type: File
outputs:
  the_output:
    outputSource: cat/out_file1
steps:
  cat:
    tool_id: cat1
    in:
      input1: the_input
report:
  markdown: 42
