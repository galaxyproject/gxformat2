class: GalaxyWorkflow
label: No annotation workflow
inputs:
  the_input:
    type: File
outputs:
  the_output:
    outputSource: cat/out_file1
steps:
  cat:
    tool_id: cat1
    doc: cat doc
    in:
      input1: the_input
