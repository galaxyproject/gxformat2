class: GalaxyWorkflow
label: Bad string default
inputs:
  the_input:
    type: string
    default: 42
outputs:
  the_output:
    outputSource: cat/out_file1
steps:
  cat:
    tool_id: cat1
    in:
      input1: the_input
