class: GalaxyWorkflow
inputs:
  test_input: data
outputs: {}
steps:
  first_cat:
    tool_id: cat1
    in:
      input1: test_input
  the_pause:
    type: pause
    in:
      input: first_cat/out_file1
